# admin-web

Placeholder web admin shell for standalone Gutenberg integration.

MVP rules:
- Use canonical SDK-backed stores.
- Do not use `@wordpress/core-data` for CRUD in phase 1.
- Configure `@wordpress/api-fetch` middleware for auth, refresh, and tracing.
