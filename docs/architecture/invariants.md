---
title: Invariants
---

# Invariants

The API and publish flows rely on a few invariants (capabilities, error envelope, preview expiry, release immutability).

Source of truth:

- `packages/domain/src/invariants.js`
- `apps/api-edge/src/http.js`
